extract colors:
  enabled: yes

make thumbnails:
  enabled: true
  sizes: [50, 330, 420, 700, 1000]
  # TODO be able to do <dist source basename>@<size>.webp (or suffix: @<size>.webp)
  file name template: <work id>/<block id>@<size>.webp

media:
  at: dist/media

build metadata file: database/.lastbuild.json
scattered mode folder: .portfoliodb/
